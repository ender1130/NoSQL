
1.如果程序直连mongo去写数据，量大的话(百万千万级别)会导致mongo处理不过来，直接导致CPU100%。
处理办法是把写的数据放入消息队列(activemq 或 rabbitmq 等)，从队列中取出再写入mongo，利用队
列削峰限流。

2.如果mongo中存储的数据过大(千万条记录以上)会导致查询非常缓慢(执行查询命令就可以感觉到)，如
果用程序去查询数据时势必会导致程序非常缓慢，mongo查询进程越积越多，最后导致mongo的CPU100%。
处理办法是在mongo的表上加索引，提高查询速度。

